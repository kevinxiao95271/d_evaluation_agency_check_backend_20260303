# 加分配置数据库化实现总结

## 需求背景

将主任演讲加分和秘书参与加分从 `application.yml` 配置文件移到数据库表中，实现：
- ✅ 通过管理后台动态配置，无需重启服务
- ✅ 实时生效，灵活调整
- ✅ 统一使用 `system_config` 表管理

## 实现方案

### 1. 数据库表设计

**使用现有表**: `system_config`

**新增配置项**:
| configKey | configName | configValue | description |
|-----------|-----------|-------------|-------------|
| director_bonus | 主任演讲加分 | 8/10 | 主任/副主任到场演讲的加分分值 |
| secretary_bonus | 秘书参与加分 | 2/3 | 会务秘书参与的加分分值 |

### 2. 后端实现

#### 2.1 Service 层 - ScoreServiceImpl.java

**修改前**:
```java
@Value("${evaluation.director-bonus:8}")
private Integer directorBonus;

@Value("${evaluation.secretary-bonus:2}")
private Integer secretaryBonus;

// 直接使用字段
Integer directorBonusScore = bonus.getDirectorPresentation() != null && bonus.getDirectorPresentation() == 1 
    ? directorBonus : 0;
```

**修改后**:
```java
@Autowired
private SystemConfigRepository systemConfigRepository;

// 从数据库读取加分配置，如果不存在则使用默认值
private Integer getDirectorBonus() {
    return systemConfigRepository.findByConfigKey("director_bonus")
        .map(config -> Integer.parseInt(config.getConfigValue()))
        .orElse(8);
}

private Integer getSecretaryBonus() {
    return systemConfigRepository.findByConfigKey("secretary_bonus")
        .map(config -> Integer.parseInt(config.getConfigValue()))
        .orElse(2);
}

// 使用方法调用
Integer directorBonusScore = bonus.getDirectorPresentation() != null && bonus.getDirectorPresentation() == 1 
    ? getDirectorBonus() : 0;
```

#### 2.2 Controller 层 - SystemConfigController.java

**新增接口**:

1. **获取加分配置**
   ```http
   GET /api/system-config/bonus-config
   ```
   
   **响应**:
   ```json
   {
       "code": 200,
       "message": "success",
       "data": {
           "directorBonus": 8,
           "secretaryBonus": 2
       }
   }
   ```

2. **更新加分配置**
   ```http
   PUT /api/system-config/bonus-config?directorBonus=10&secretaryBonus=3
   ```
   
   **请求参数**:
   - `directorBonus` (可选): 主任演讲加分分值
   - `secretaryBonus` (可选): 秘书参与加分分值
   
   **响应**:
   ```json
   {
       "code": 200,
       "message": "success"
   }
   ```

#### 2.3 Controller 层 - SystemController.java

**修改内容**:
```java
@Autowired
private SystemConfigRepository systemConfigRepository;

private Integer getDirectorBonus() {
    return systemConfigRepository.findByConfigKey("director_bonus")
        .map(config -> Integer.parseInt(config.getConfigValue()))
        .orElse(8);
}

private Integer getSecretaryBonus() {
    return systemConfigRepository.findByConfigKey("secretary_bonus")
        .map(config -> Integer.parseInt(config.getConfigValue()))
        .orElse(2);
}

@GetMapping("/config")
public Result<Map<String, Object>> getConfig() {
    Map<String, Object> config = new HashMap<>();
    // ...
    config.put("directorBonus", getDirectorBonus());  // 从数据库读取
    config.put("secretaryBonus", getSecretaryBonus());  // 从数据库读取
    return Result.success(config);
}
```

## API 接口清单

### 已有接口（保持不变）

1. **查询附加项**
   - `GET /api/bonus?taskId={taskId}&institutionId={institutionId}`
   - 返回：`directorPresentation`, `secretaryParticipation` (0 或 1)

2. **保存附加项**
   - `POST /api/bonus`
   - 设置机构是否参与主任演讲和秘书参与

### 新增接口

3. **获取加分配置** ⭐ NEW
   - `GET /api/system-config/bonus-config`
   - 返回：`directorBonus`, `secretaryBonus` (分值)

4. **更新加分配置** ⭐ NEW
   - `PUT /api/system-config/bonus-config?directorBonus=10&secretaryBonus=3`
   - 更新数据库中的加分分值

5. **系统配置（已修改）**
   - `GET /api/system/config`
   - 返回的配置现在从数据库读取

## 测试验证

### 测试环境
- 服务端口：5031
- 测试任务：taskId = 1
- 测试机构：institutionId = 1

### 测试 1：获取当前加分配置

**请求**:
```http
GET /api/system-config/bonus-config
```

**结果**:
```json
{
    "data": {
        "directorBonus": 8,      ✅ (默认值)
        "secretaryBonus": 2      ✅ (默认值)
    }
}
```

### 测试 2：更新加分配置

**请求**:
```http
PUT /api/system-config/bonus-config?directorBonus=10&secretaryBonus=3
```

**结果**:
```json
{
    "code": 200,
    "message": "success"        ✅
}
```

### 测试 3：验证配置已更新

**请求**:
```http
GET /api/system-config/bonus-config
```

**结果**:
```json
{
    "data": {
        "directorBonus": 10,     ✅ (已更新)
        "secretaryBonus": 3      ✅ (已更新)
    }
}
```

### 测试 4：验证评分计算使用新配置

**请求**:
```http
GET /api/score/result?taskId=1&institutionId=1
```

**结果**:
```json
{
    "data": {
        "institutionName": "临床检验中心",
        "directorBonus": 10,     ✅ (使用新配置)
        "secretaryBonus": 3,     ✅ (使用新配置)
        "finalScore": 93.06      ✅ (包含新的加分)
    }
}
```

**对比**:
- 修改前总分：80.06 (8+2 加分)
- 修改后总分：93.06 (10+3 加分)
- 差异：+13 分 ✅

## 使用流程

### 管理员操作流程

1. **登录管理后台**
2. **进入系统配置 → 加分配置**
3. **查看当前配置**
   - 主任演讲加分：8 分
   - 秘书参与加分：2 分

4. **修改配置（如需要）**
   - 修改主任演讲加分：10 分
   - 修改秘书参与加分：3 分
   - 点击"保存"

5. **立即生效**
   - 无需重启服务 ✅
   - 所有后续评分计算自动使用新配置 ✅

### 评分计算流程

```
1. 机构勾选加分项
   ↓
2. 保存附加项 (InstitutionBonus)
   directorPresentation = 1
   secretaryParticipation = 1
   ↓
3. 计算评分时
   ↓
4. 从数据库读取配置
   director_bonus = 10 (从 system_config)
   secretary_bonus = 3 (从 system_config)
   ↓
5. 计算最终总分
   finalScore = expertContribution + publicContribution + 10 + 3
```

## 优势对比

### 修改前（配置文件）

```yaml
# application.yml
evaluation:
  director-bonus: 8
  secretary-bonus: 2
```

**缺点**:
- ❌ 修改后需要重启服务
- ❌ 无法动态调整
- ❌ 需要运维人员操作
- ❌ 容易出错

### 修改后（数据库配置）

```sql
-- system_config 表
INSERT INTO system_config (config_key, config_name, config_value, description)
VALUES 
  ('director_bonus', '主任演讲加分', '10', '主任/副主任到场演讲的加分分值'),
  ('secretary_bonus', '秘书参与加分', '3', '会务秘书参与的加分分值');
```

**优点**:
- ✅ 无需重启服务
- ✅ 实时生效
- ✅ 管理后台可操作
- ✅ 权限控制
- ✅ 有操作记录

## 数据初始化

### 首次部署

系统会自动使用默认值：
- 主任演讲加分：8 分
- 秘书参与加分：2 分

当第一次通过 API 更新时，会自动创建配置记录。

### 手动初始化 SQL

```sql
-- 插入默认配置
INSERT INTO system_config (config_key, config_name, config_value, description, create_time, update_time)
VALUES 
  ('director_bonus', '主任演讲加分', '8', '主任/副主任到场演讲的加分分值', NOW(), NOW()),
  ('secretary_bonus', '秘书参与加分', '2', '会务秘书参与的加分分值', NOW(), NOW())
ON DUPLICATE KEY UPDATE 
  config_value = VALUES(config_value);
```

## 注意事项

### 1. 配置优先级

**数据库配置 > 配置文件默认值**

- 如果数据库中有配置，使用数据库的值
- 如果数据库中没有配置，使用默认值（8 分和 2 分）

### 2. 数据类型

- 配置值存储为字符串 (`String`)
- 使用时转换为整数 (`Integer`)
- 如果转换失败会抛出异常

### 3. 缓存问题

- 当前实现每次计算都从数据库读取
- 未来可以考虑添加缓存（如 Redis）
- 或使用 `@Cacheable` 注解

### 4. 并发安全

- 多个用户同时修改配置时，后提交的成功
- 建议添加乐观锁或版本控制
- 管理后台应显示最后修改人和时间

## 相关文件清单

### 修改的文件（3 个）

1. **ScoreServiceImpl.java** - Service 层读取逻辑
2. **SystemController.java** - 系统配置接口
3. **SystemConfigController.java** - 配置管理接口

### 涉及的表（1 个）

1. **system_config** - 系统配置表

---

**完成时间**: 2026-03-05  
**验证人**: AI Assistant  
**服务状态**: ✅ 正常运行（端口 5031）  
**配置状态**: ✅ 数据库配置已生效（主任 10 分，秘书 3 分）
