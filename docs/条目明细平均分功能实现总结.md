# 条目明细专家/大众平均分功能实现总结

## 功能概述
为评分结果的条目明细（`ItemResultDTO`）添加了专家平均分和大众平均分字段，确保分数完全来自数据库真实评分记录，无记录时返回 0。

## 修改内容

### 1. DTO 层 - `ScoreResultDTO.java`
**文件路径**: `src/main/java/com/zjmc/evaluation/dto/ScoreResultDTO.java`

**新增字段**:
- `expertAvgScore` - 专家评委平均分
- `publicAvgScore` - 大众评委平均分  
- `avgScore` - 总体平均分（重命名自原 `avgScore`）

```java
@Data
@Schema(description = "条目结果 DTO")
public static class ItemResultDTO {
    @Schema(description = "条目 ID")
    private Long itemId;

    @Schema(description = "条目名称")
    private String itemName;

    @Schema(description = "所属分类名称")
    private String categoryName;

    @Schema(description = "条目满分")
    private Double maxScore;

    @Schema(description = "专家评委平均分")
    private Double expertAvgScore;      // 新增

    @Schema(description = "大众评委平均分")
    private Double publicAvgScore;      // 新增

    @Schema(description = "总体平均分")
    private Double avgScore;            // 重命名
}
```

### 2. Service 层 - `ScoreServiceImpl.java`
**文件路径**: `src/main/java/com/zjmc/evaluation/service/impl/ScoreServiceImpl.java`

**核心改动**:

#### 2.1 重构 `calculateItemResults` 方法
- **从模板获取所有条目**: 确保即使没有评分记录的条目也会显示
- **按评委类型拆分计算**: 分别计算专家和大众的平均分
- **补齐所有条目**: 无记录的条目返回 0 分

```java
private List<ScoreResultDTO.ItemResultDTO> calculateItemResults(List<ScoreRecord> records, ScoreTemplate template) {
    // 获取模板中的所有条目，确保即使没有评分记录的条目也会显示
    List<ScoreItem> allItems = scoreItemRepository.findByTemplateIdAndStatusOrderBySortOrderAsc(
        template.getId(), 1);
    
    // 将评分记录按 itemId 分组
    Map<Long, List<ScoreRecord>> itemRecordsMap = records.stream()
        .collect(Collectors.groupingBy(r -> r.getItem().getId()));

    List<ScoreResultDTO.ItemResultDTO> results = new ArrayList<>();
    
    // 遍历所有模板条目，确保补齐所有条目
    for (ScoreItem item : allItems) {
        List<ScoreRecord> itemRecords = itemRecordsMap.getOrDefault(item.getId(), new ArrayList<>());
        
        ScoreResultDTO.ItemResultDTO dto = new ScoreResultDTO.ItemResultDTO();
        dto.setItemId(item.getId());
        dto.setItemName(item.getName());
        dto.setCategoryName(item.getCategory() != null ? item.getCategory().getName() : "");
        dto.setMaxScore(item.getMaxScore());
        
        if (itemRecords.isEmpty()) {
            // 无评分记录时，所有分数为 0
            dto.setExpertAvgScore(0.0);
            dto.setPublicAvgScore(0.0);
            dto.setAvgScore(0.0);
        } else {
            // 分离专家和大众的评分记录
            List<ScoreRecord> expertRecords = itemRecords.stream()
                .filter(r -> r.getJudge().getType() == Judge.JudgeType.EXPERT)
                .collect(Collectors.toList());
            
            List<ScoreRecord> publicRecords = itemRecords.stream()
                .filter(r -> r.getJudge().getType() == Judge.JudgeType.PUBLIC)
                .collect(Collectors.toList());
            
            // 计算专家平均分（按评委去重后平均）
            Double expertAvg = calculateItemAverageByJudges(expertRecords);
            dto.setExpertAvgScore(expertAvg);
            
            // 计算大众平均分（按评委去重后平均）
            Double publicAvg = calculateItemAverageByJudges(publicRecords);
            dto.setPublicAvgScore(publicAvg);
            
            // 计算总体平均分（所有评委）
            Double overallAvg = calculateItemAverageByJudges(itemRecords);
            dto.setAvgScore(overallAvg);
        }
        
        results.add(dto);
    }

    return results;
}
```

#### 2.2 新增 `calculateItemAverageByJudges` 方法
用于计算条目的平均分，支持以下特性:
- **按评委去重**: 每个评委的多次评分取平均
- **去头去尾规则**: 超过 2 个评委时去掉最高分和最低分
- **空记录处理**: 无记录时返回 0.0

```java
/**
 * 计算条目的平均分（按评委去重，支持去头去尾）
 */
private Double calculateItemAverageByJudges(List<ScoreRecord> records) {
    if (records.isEmpty()) {
        return 0.0;
    }

    // 按评委分组，计算每个评委的平均分
    Map<Long, List<ScoreRecord>> judgeRecords = records.stream()
        .collect(Collectors.groupingBy(r -> r.getJudge().getId()));

    List<Double> judgeScores = new ArrayList<>();
    for (List<ScoreRecord> recs : judgeRecords.values()) {
        Double avg = recs.stream().mapToDouble(ScoreRecord::getScore).average().orElse(0.0);
        judgeScores.add(avg);
    }

    Double finalAvg;
    if (judgeScores.size() <= 2) {
        // 评委数量不足 3 人，直接取平均
        finalAvg = judgeScores.stream().mapToDouble(Double::doubleValue).average().orElse(0.0);
    } else {
        // 去掉最高分和最低分
        Collections.sort(judgeScores);
        List<Double> trimmed = judgeScores.subList(1, judgeScores.size() - 1);
        finalAvg = trimmed.stream().mapToDouble(Double::doubleValue).average().orElse(0.0);
    }
    
    return finalAvg;
}
```

## 数据保证

### ✅ 真实性保证
- **数据来源**: 所有分数仅从 `score_record` 表汇总
- **不造假数据**: 无评分记录时自然返回 0，而非编造数据
- **模板对齐**: 条目清单来自评分模板，确保完整性

### ✅ 计算逻辑
1. **专家平均分**: 仅统计 `JudgeType.EXPERT` 的评分记录
2. **大众平均分**: 仅统计 `JudgeType.PUBLIC` 的评分记录
3. **总体平均分**: 统计所有评委的评分记录
4. **去重规则**: 按评委 ID 去重，每个评委的多条记录取平均后再参与总体计算
5. **去头去尾**: 超过 2 个评委时，去掉最高分和最低分后取平均

## API 接口

### 接口地址
```
GET /api/score/result?taskId={taskId}&institutionId={institutionId}
```

### 返回示例
```json
{
    "code": 200,
    "message": "success",
    "data": {
        "taskId": 1,
        "institutionName": "某某机构",
        "expertAvgScore": 90.01,
        "publicAvgScore": 85.24,
        "finalScore": 80.06,
        "itemResults": [
            {
                "itemId": 1,
                "itemName": "组织与人员配备",
                "categoryName": "基础管理",
                "maxScore": 10.0,
                "expertAvgScore": 9.14,      // 专家平均分
                "publicAvgScore": 8.58,      // 大众平均分
                "avgScore": 8.91             // 总体平均分
            },
            // ... 更多条目
        ]
    }
}
```

## 验证步骤

### 1. 编译检查
```bash
mvn -q -DskipTests package
```
✅ 编译成功

### 2. 启动服务
```bash
mvn spring-boot:run
```
✅ 服务启动成功，端口 5031 已监听

### 3. API 测试
```powershell
$response = Invoke-WebRequest -Uri "http://localhost:5031/api/score/result?taskId=1&institutionId=1" -Method GET
$json = $response.Content | ConvertFrom-Json
$json.data.itemResults[0] | ConvertTo-Json
```

### 测试结果
```json
{
    "itemId": 1,
    "itemName": "组织与人员配备",
    "categoryName": "基础管理",
    "maxScore": 10.0,
    "expertAvgScore": 9.142727272727273,     ✅
    "publicAvgScore": 8.58125,               ✅
    "avgScore": 8.906190476190476            ✅
}
```

## 关键改进

| 改进点 | 之前 | 现在 |
|--------|------|------|
| 专家平均分 | ❌ 无 | ✅ 有 |
| 大众平均分 | ❌ 无 | ✅ 有 |
| 条目完整性 | ⚠️ 仅有记录的条目 | ✅ 模板所有条目 |
| 无记录处理 | ❌ 不显示 | ✅ 显示为 0 |
| 数据来源 | ✅ 真实记录 | ✅ 真实记录 |

## 使用说明

### 前端调用
```javascript
// 获取单个机构的评分结果（包含条目明细）
fetch('http://localhost:5031/api/score/result?taskId=1&institutionId=1')
  .then(res => res.json())
  .then(data => {
    console.log('机构总分:', data.data.finalScore);
    console.log('条目明细:');
    data.data.itemResults.forEach(item => {
      console.log(`  ${item.itemName}:`);
      console.log(`    专家平均分：${item.expertAvgScore}`);
      console.log(`    大众平均分：${item.publicAvgScore}`);
      console.log(`    总体平均分：${item.avgScore}`);
    });
  });
```

## 注意事项

1. **TOTAL 模式兼容**: 如果评分使用 TOTAL 模式（仅总分），`itemResults` 会为空数组，因为该模式下没有逐条评分记录
2. **评委数量规则**: 评委数量 ≤ 2 时直接平均，> 2 时去掉最高分和最低分
3. **性能优化**: 基于模板条目遍历，避免 N+1 查询问题
4. **数据一致性**: 所有分数均来自 `score_record` 表，保证与总分计算一致

## 部署状态

- ✅ 编译通过
- ✅ 服务运行中（端口 5031）
- ✅ API 测试通过
- ✅ 数据验证通过

---

**完成时间**: 2026-03-05  
**验证人**: AI Assistant
