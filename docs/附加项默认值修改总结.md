# 附加项默认值修改总结

## 修改需求

将主任演讲加分和秘书参与加分的默认值从 **未勾选（0）** 改为 **已勾选（1）**，简化操作流程。

## 修改内容

### 1. 实体类 - InstitutionBonus.java

**文件路径**: `src/main/java/com/zjmc/evaluation/entity/InstitutionBonus.java`

**修改前**:
```java
@Column(nullable = false)
@Schema(description = "是否有主任/副主任到场演讲：0-否，1-是（+8 分）")
private Integer directorPresentation = 0;

@Column(nullable = false)
@Schema(description = "是否有人参与会务秘书：0-否，1-是（+2 分）")
private Integer secretaryParticipation = 0;
```

**修改后**:
```java
@Column(nullable = false)
@Schema(description = "是否有主任/副主任到场演讲：0-否，1-是（+8 分）")
private Integer directorPresentation = 1;  // ✅ 改为 1

@Column(nullable = false)
@Schema(description = "是否有人参与会务秘书：0-否，1-是（+2 分）")
private Integer secretaryParticipation = 1;  // ✅ 改为 1
```

### 2. Service 实现 - InstitutionBonusServiceImpl.java

**文件路径**: `src/main/java/com/zjmc/evaluation/service/impl/InstitutionBonusServiceImpl.java`

#### 2.1 save() 方法

**修改前**:
```java
bonus.setDirectorPresentation(dto.getDirectorPresentation() != null ? dto.getDirectorPresentation() : 0);
bonus.setSecretaryParticipation(dto.getSecretaryParticipation() != null ? dto.getSecretaryParticipation() : 0);
```

**修改后**:
```java
// 默认值为 1（已勾选），如果传入 null 则默认为已勾选
bonus.setDirectorPresentation(dto.getDirectorPresentation() != null ? dto.getDirectorPresentation() : 1);
bonus.setSecretaryParticipation(dto.getSecretaryParticipation() != null ? dto.getSecretaryParticipation() : 1);
```

#### 2.2 findByTaskIdAndInstitutionId() 方法

**修改前**:
```java
InstitutionBonus empty = new InstitutionBonus();
empty.setDirectorPresentation(0);
empty.setSecretaryParticipation(0);
```

**修改后**:
```java
// 返回空对象，默认值为已勾选（1）
InstitutionBonus empty = new InstitutionBonus();
empty.setDirectorPresentation(1);
empty.setSecretaryParticipation(1);
```

## 影响范围

### 涉及接口

1. **查询附加项**
   - `GET /api/bonus?taskId={taskId}&institutionId={institutionId}`
   - 无记录时返回默认值：两项均为 1

2. **保存附加项**
   - `POST /api/bonus`
   - 不传分值时自动使用默认值：两项均为 1

3. **计算评分结果**
   - `GET /api/score/result?taskId={taskId}&institutionId={institutionId}`
   - 自动计算默认加分：主任演讲 8 分 + 秘书参与 2 分 = 10 分

### 业务逻辑变化

| 场景 | 修改前 | 修改后 |
|------|--------|--------|
| 新增机构附加项 | 默认不加分（0） | 默认加分（1）✅ |
| 查询无记录的机构 | 返回 0 | 返回 1 ✅ |
| 保存时不传分值 | 视为不加分（0） | 视为加分（1）✅ |
| 主动取消加分 | 需设置为 0 | 需设置为 0（不变） |

## 测试验证

### 测试环境
- 服务端口：5031
- 测试任务：taskId = 1
- 测试机构：institutionId = 10

### 测试 1：查询新机构的附加项

**请求**:
```http
GET /api/bonus?taskId=1&institutionId=10
```

**预期结果**: 默认值为已勾选

**实际结果**:
```json
{
    "data": {
        "directorPresentation": 1,      ✅
        "secretaryParticipation": 1     ✅
    }
}
```

### 测试 2：保存时不传分值

**请求**:
```http
POST /api/bonus
Content-Type: application/json

{
    "taskId": 1,
    "institutionId": 10,
    "filledBy": "admin"
}
```

**预期结果**: 自动使用默认值 1

**实际结果**:
```json
{
    "data": {
        "directorPresentation": 1,      ✅
        "secretaryParticipation": 1,    ✅
        "filledBy": "admin"
    }
}
```

### 测试 3：验证评分结果自动包含加分

**请求**:
```http
GET /api/score/result?taskId=1&institutionId=10
```

**预期结果**: 自动计算加分

**实际结果**:
```json
{
    "data": {
        "institutionName": "省医疗质控中心",
        "directorBonus": 8,             ✅ (自动加 8 分)
        "secretaryBonus": 2,            ✅ (自动加 2 分)
        "finalScore": 89.11             ✅ (包含 10 分加分)
    }
}
```

## 使用说明

### 修改前的问题

用户需要手动为每个机构勾选加分项：
1. 进入附加项管理页面
2. 选择任务和机构
3. **勾选"主任演讲"** ← 额外步骤
4. **勾选"秘书参与"** ← 额外步骤
5. 保存

### 修改后的优势

**简化流程**：
1. 进入附加项管理页面
2. 选择任务和机构
3. ~~勾选~~ **无需勾选，默认已加上！** ✅
4. 保存（或直接查看结果）

### 如何取消加分

如果某个机构不需要加分，需要手动设置为 0：

**方法 1：通过 API**
```http
POST /api/bonus
Content-Type: application/json

{
    "taskId": 1,
    "institutionId": 1,
    "directorPresentation": 0,    // 明确设置为 0
    "secretaryParticipation": 0,  // 明确设置为 0
    "filledBy": "admin"
}
```

**方法 2：通过管理后台**
- 在附加项管理页面
- 取消勾选对应的加分项
- 保存即可

## 注意事项

### 1. 数据库历史数据

**重要**: 修改默认值不影响已保存的历史数据
- 之前保存为 0 的记录仍然是 0
- 之前保存为 1 的记录仍然是 1
- **只影响新增记录和未设置的情况**

### 2. 业务流程建议

**推荐操作流程**:
1. 创建任务并添加机构后
2. 系统自动为所有机构启用加分（默认 1）
3. 管理员根据实际情况**取消**不符合条件的机构加分
4. 查看评分结果

**而不是**:
1. 创建任务并添加机构
2. 逐个为机构**添加**加分 ✖️

### 3. 分值配置

- **默认分值**: 仍然在 `application.yml` 中配置
  - `director-bonus: 8`
  - `secretary-bonus: 2`
- **一般无需修改**，除非政策调整

## 部署状态

- ✅ 编译成功
- ✅ 服务运行正常（端口 5031）
- ✅ 所有测试通过
- ✅ 默认加分已生效

## 相关文件清单

### 修改的文件（2 个）
1. `InstitutionBonus.java` - 实体类默认值修改
2. `InstitutionBonusServiceImpl.java` - Service 层默认值处理

### 涉及的接口（3 个）
1. `GET /api/bonus` - 查询附加项
2. `POST /api/bonus` - 保存附加项
3. `GET /api/score/result` - 计算评分结果

---

**完成时间**: 2026-03-05  
**验证人**: AI Assistant  
**服务状态**: ✅ 正常运行（端口 5031）
